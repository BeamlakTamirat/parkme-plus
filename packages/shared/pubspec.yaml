name: shared
description: Shared package - MINIMAL VERSION
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  
  # ONLY ESSENTIAL DEPENDENCIES
  appwrite: ^12.0.1
  flutter_riverpod: ^2.5.1
  flutter_dotenv: ^5.1.0
  http: ^1.1.0
  
  # Added missing dependencies
  dio: ^5.4.0
  uuid: ^4.2.1
  intl: ^0.19.0
  
  # Mapbox Maps integration (REAL WORLD MAPS) - Interactive Maps
  mapbox_maps_flutter: ^2.0.0
  geolocator: ^10.1.0
  geocoding: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter: